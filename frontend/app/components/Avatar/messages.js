/*
 * Avatar Messages
 *
 * This contains all the text for the Avatar component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Avatar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Avatar',
  },
});
